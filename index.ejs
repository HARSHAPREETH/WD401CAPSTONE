<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bike Details</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background-image: url('http://getwallpapers.com/wallpaper/full/7/6/6/873704-most-popular-sports-bikes-wallpapers-1920x1080-for-hd.jpg');
      background-size: cover;
      background-repeat: no-repeat;
      background-attachment: fixed;
      font-family: Arial, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      backdrop-filter: blur(10px);
    }

    .card {
      background-color: rgba(255, 255, 255, 0.9);
      width: 300px;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      text-align: center;
    }

    .card h2 {
      margin-bottom: 20px;
    }

    .input-container {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .input-container input {
      width: 45%;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }

    .card button {
      background-color: #007BFF;
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 5px;
      cursor: pointer;
      font-weight: bold;
      transition: background-color 0.3s ease-in-out;
    }

    .card button:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <form action="/bikeinfo"method="post">
    <div class="card">
      <h2>Bike Details</h2>
      <div class="input-container">
        <input type="text" id="bikeName" name="bikeName" placeholder="Bike Name" required>
        <input type="text" id="bikeModel" name="bikeModel" placeholder="Bike Model" required>
      </div>
      <button type="submit">Search</button>
    </div>
  </form>

  <% if (bikeInfo) { %>
    <div class="card">
      <h2>Bike Details</h2>
      <p><strong>Make:</strong> <%= bikeInfo.make %></p>
      <p><strong>Model:</strong> <%= bikeInfo.model %></p>
      <p><strong>Type:</strong> <%= bikeInfo.type %></p>
      <p><strong>Year:</strong> <%= bikeInfo.year %></p>
      <p><strong>Power:</strong> <%= bikeInfo.power %></p>
      <p><strong>Fuel Control:</strong> <%= bikeInfo.fuel_control %></p>
      <p><strong>Gear Box:</strong> <%= bikeInfo.gearbox %></p>
    </div>
  <% } else if (error) { %>
    <div class="card">
      <p><%= error %></p>
    </div>
  <% } %>
</body>
</html>
